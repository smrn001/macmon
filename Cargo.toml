[package]
name = "macmon"
version = "0.1.0"
edition = "2024"
description = "Ultra-lightweight system monitor for macOS"

[dependencies]
crossterm = "0.29.0"
ratatui = "0.30.2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
